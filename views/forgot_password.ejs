<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password - DREAMURLIFEWITHME</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* Reusing form-container styles */
        .form-container {
            background-color: rgba(0, 0, 0, 0.6);
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.4);
            width: 100%;
            max-width: 500px;
            margin: 20px auto;
            text-align: left;
        }
        .form-container h2 {
            text-align: center;
            color: white;
            margin-bottom: 20px;
        }
        .form-group {
            margin-bottom: 15px;
        }
        .form-group label {
            display: block;
            margin-bottom: 5px;
            color: white;
        }
        .form-group input[type="tel"] {
            width: calc(100% - 20px);
            padding: 10px;
            border: 1px solid #555;
            border-radius: 4px;
            background-color: #333;
            color: white;
        }
        .form-group input[type="tel"]:focus {
            outline: none;
            border-color: #888;
        }
        .form-container button {
            width: 100%;
            padding: 10px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1.1em;
            transition: background-color 0.3s ease;
        }
        .form-container button:hover {
            background-color: #45a049;
        }
        .message {
            text-align: center;
            margin-top: 15px;
            color: yellow;
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h2>Forgot Password</h2>
        <% if (message) { %>
            <p class="message"><%= message %></p>
        <% } %>
        <form action="/forgot-password" method="POST">
            <div class="form-group">
                <label for="contact">Enter your Contact Number:</label>
                <input type="tel" id="contact" name="contact" pattern="[0-9]{10}" placeholder="e.g., 1234567890" required>
            </div>
            <button type="submit">Reset Password</button>
        </form>
        <p style="text-align: center; margin-top: 20px;"><a href="/login" style="color: #86a8e7; text-decoration: none;">Back to Login</a></p>
    </div>
</body>
</html>